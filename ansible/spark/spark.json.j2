{
    "LighthouseServer": "{{ lighthouse_server }}",
    "AcceptedJobs": [
        {% for job_type in accepted_job_types %}"{{ job_type }}"{%+ if not loop.last %},

        {%+ else %}

        {% endif %}
     {% endfor %}
    ],
    "DputHost": "{{ dput_profile }}",
    "GpgKeyID": "{{ gpg_fingerprint }}"
}
