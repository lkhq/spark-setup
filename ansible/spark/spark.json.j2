{
    "LighthouseServer": "{{ item.lighthouse_server }}",
    "AcceptedJobs": [
        "iso-image-build",
        "package-build"
    ],
    "DputHost": "{{ item.dput_profile }}",
    "GpgKeyUID": "{{ item.gpg_fingerprint }}"
}
